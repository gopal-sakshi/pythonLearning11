Have a dedicated virtual environment for each Django project
one way to manage a virtual environment is venv, which is included in Python

source env23/bin/activate

You must activate the virtual environment every time you open the command prompt to work on your project.


install Django while you are in the virtual environment!